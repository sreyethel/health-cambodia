import { ToCalendarPipe } from './to-calendar.pipe';

describe('ToCalendarPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCalendarPipe();
    expect(pipe).toBeTruthy();
  });
});
